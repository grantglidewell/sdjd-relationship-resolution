import theme from "mdx-deck/themes";

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  font: "sans-serif",
  // custom colors
  colors: {
    text: "#090807",
    background: "#efefef",
    link: "#070809"
  }
};
